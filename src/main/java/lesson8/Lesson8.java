package lesson8;

public class Lesson8 {
    // List - интерфейс в которм есть методы
    // List implementations. LinkedList.

    // ArrayList
    // get(index) set(index) = O(1)
    // add (index, value) = O(n)
    //remove = O(n)

    //LinkedList
    //get(index) set(index) = O(n)
    // add (index, value) в середину = O(n)
    // add (0, value) add(tail, value) = O(1)
    // remove(index) из середины = O(n)
    // remove(0) remove(tail) = O(1)
}
